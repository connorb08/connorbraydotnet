{
	"name": "admin",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "react-router build",
		"typegen": "wrangler types -c wrangler.json -c $DATABASE_DIR/wrangler.json && react-router typegen",
		"deploy": "bun run build && wrangler deploy",
		"dev": "react-router dev",
		"postinstall": "bun cf-typegen",
		"preview": "bun run build && vite preview",
		"typecheck": "bun typegen && react-router typegen && tsc -b"
	},
	"devDependencies": {
		"@tailwindcss/vite": "^4.1.4",
		"tailwindcss": "^4.1.4",
		"vite-tsconfig-paths": "^5.1.4"
	},
	"peerDependencies": {
		"isbot": "*",
		"react": "*",
		"react-dom": "*",
		"react-router": "*",
		"wrangler": "*",
		"typescript": "*",
		"vite": "*",
		"@cloudflare/vite-plugin": "*",
		"@react-router/dev": "*"
	}
}
