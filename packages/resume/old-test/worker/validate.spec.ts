// import type MainEntrypoint from "#worker";
// import { SELF } from "cloudflare:test";
// import { ValidateTestData } from "#test/validation-tests";

// describe("Cloudflare Worker Should Validate", ({ skip }) => {
// 	const worker = SELF as unknown as MainEntrypoint;
// 	Object.entries(ValidateTestData(worker.validate)).forEach(
// 		async ([should, assert]) => {
// 			it(should, assert);
// 		},
// 	);
// });
