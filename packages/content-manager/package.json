{
	"name": "content-manager",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev --experimental-images-local-mode",
		"typegen": "wrangler types",
		"seed": "bun run seed.ts",
		"format": "biome format",
		"lint": "biome lint",
		"typecheck": "tsc -b && tsc -p backend/tsconfig.json",
		"depcheck": "bunx depcheck"
	}
}
